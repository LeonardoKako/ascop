import styled from "styled-components";
import { colors, Desc } from "../../styles";

export const FooterContainer = styled.div`
  padding: 60px 240px;
  background-color: ${colors.darkBlue};

  ${Desc} {
    text-align: center;
  }
`;
